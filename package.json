{
  "name": "dis-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare-logs": "mkdir -p logs",
    "dev": "node --watch --experimental-strip-types index.ts",
    "dev-logging": "npm run prepare-logs && npm run dev >> logs/log-$(date +%Y-%m-%d).txt 2>&1",
    "dev-logging-watch": "npm run dev-logging & tail -f logs/log-$(date +%Y-%m-%d).txt",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "type": "module",
  "license": "ISC",
  "dependencies": {
    "@discordjs/opus": "^0.9.0",
    "@discordjs/voice": "^0.17.0",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5",
    "ffmpeg-static": "^5.2.0",
    "libsodium-wrappers": "^0.7.15",
    "lowdb": "^7.0.1",
    "opusscript": "^0.1.1",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
